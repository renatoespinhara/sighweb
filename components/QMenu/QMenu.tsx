import React from 'react';

/**
 * QMenu
 * Menu de contexto / dropdown.
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QMenu.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QMenuProps {
  open?: boolean;
}

export function QMenu({ open = false }: QMenuProps) {
  return (
    <div className="q-qmenu">
      {/* TODO: implementar QMenu */}
    </div>
  );
}

export default QMenu;
