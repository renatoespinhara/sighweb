import React from 'react';

/**
 * QButtonGroup
 * Agrupamento de botões (segmented control).
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QButtonGroup.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QButtonGroupProps {
  orientation?: 'horizontal' | 'vertical';
}

export function QButtonGroup({ orientation = 'horizontal' }: QButtonGroupProps) {
  return (
    <div className="q-qbuttongroup">
      {/* TODO: implementar QButtonGroup */}
    </div>
  );
}

export default QButtonGroup;
