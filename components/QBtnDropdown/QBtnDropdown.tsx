import React from 'react';

/**
 * QBtnDropdown
 * Botão com menu suspenso.
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QBtnDropdown.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QBtnDropdownProps {
  label?: string;
  open?: boolean;
}

export function QBtnDropdown({ label, open = false }: QBtnDropdownProps) {
  return (
    <div className="q-qbtndropdown">
      {/* TODO: implementar QBtnDropdown */}
    </div>
  );
}

export default QBtnDropdown;
