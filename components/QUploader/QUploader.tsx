import React from 'react';

/**
 * QUploader
 * Componente de upload de arquivos.
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QUploader.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QUploaderProps {
  multiple?: boolean;
}

export function QUploader({ multiple = false }: QUploaderProps) {
  return (
    <div className="q-quploader">
      {/* TODO: implementar QUploader */}
    </div>
  );
}

export default QUploader;
