import React from 'react';

/**
 * QTooltip
 * Tooltip de apoio contextual.
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QTooltip.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QTooltipProps {
  text?: string;
}

export function QTooltip({ text }: QTooltipProps) {
  return (
    <div className="q-qtooltip">
      {/* TODO: implementar QTooltip */}
    </div>
  );
}

export default QTooltip;
