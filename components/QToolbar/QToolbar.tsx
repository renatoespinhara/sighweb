import React from 'react';

/**
 * QToolbar
 * Barra de ferramentas / topo de tela.
 *
 * STUB gerado automaticamente. Substitua pelo markup/estilo reais do design system.
 * Depois de implementar, o arquivo QToolbar.figma.tsx (nesta mesma pasta) já está
 * pronto para conectar este componente ao node correspondente no Figma via Code Connect.
 */
export interface QToolbarProps {
  title?: string;
}

export function QToolbar({ title }: QToolbarProps) {
  return (
    <div className="q-qtoolbar">
      {/* TODO: implementar QToolbar */}
    </div>
  );
}

export default QToolbar;
